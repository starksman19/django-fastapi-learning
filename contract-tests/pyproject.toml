[project]
name = "contract-tests"
version = "0.1.0"
description = "HTTP contract tests shared by the reference services and their ports."
requires-python = ">=3.12,<3.14"
dependencies = [
    "pytest>=8.3,<9.0",
    "requests>=2.32,<3.0",
]

[tool.uv]
package = false
